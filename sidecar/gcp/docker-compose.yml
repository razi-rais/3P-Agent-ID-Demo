# Usage: docker compose --env-file .env up --build
# Copy .env.example to .env and fill in your values before running.

services:
  # Microsoft Entra SDK for AgentID Sidecar
  sidecar:
    image: mcr.microsoft.com/entra-sdk/auth-sidecar:1.0.0-azurelinux3.0-distroless
    container_name: agent-id-sidecar-google
    ports:
      - "5002:5000"
    environment:
      # Microsoft Entra ID Configuration - Use BLUEPRINT App ID here!
      - AzureAd__Instance=https://login.microsoftonline.com/
      - AzureAd__TenantId=${TENANT_ID}
      - AzureAd__ClientId=${BLUEPRINT_APP_ID}
      
      # Client Secret Configuration
      - AzureAd__ClientCredentials__0__SourceType=ClientSecret
      - AzureAd__ClientCredentials__0__ClientSecret=${BLUEPRINT_CLIENT_SECRET}
      
      # Downstream API configuration (Microsoft Graph)
      - DownstreamApis__graph__BaseUrl=https://graph.microsoft.com/v1.0/
      - DownstreamApis__graph__Scopes__0=https://graph.microsoft.com/.default
      - DownstreamApis__graph__RequestAppToken=true
      
      # ASP.NET Core settings
      - ASPNETCORE_ENVIRONMENT=Production
      - ASPNETCORE_URLS=http://+:5000
      - AllowedHosts=*
    networks:
      - agent-network-google

  # Weather API - Validates Agent Identity tokens
  weather-api:
    build: ../weather-api
    container_name: weather-api-google
    ports:
      - "8082:8080"
    networks:
      - agent-network-google
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/health"]
      interval: 10s
      timeout: 5s
      retries: 3

  # LLM Agent with Google Vertex AI
  llm-agent-google:
    build: .
    container_name: llm-agent-google
    ports:
      - "3002:3000"
    environment:
      - SIDECAR_URL=http://sidecar:5000
      - WEATHER_API_URL=http://weather-api:8080
      - AGENT_APP_ID=${AGENT_CLIENT_ID}
      - GCP_PROJECT_ID=${GCP_PROJECT_ID}
      - GCP_LOCATION=${GCP_LOCATION:-us-central1}
      - VERTEXAI_MODEL_ID=${VERTEXAI_MODEL_ID:-gemini-1.0-pro-002}
      - GOOGLE_APPLICATION_CREDENTIALS=/app/vertex-ai-key.json
    volumes:
      - ./vertex-ai-key.json:/app/vertex-ai-key.json:ro  # Place your GCP service account key here
    networks:
      - agent-network-google
    depends_on:
      - sidecar
      - weather-api

networks:
  agent-network-google:
    driver: bridge
